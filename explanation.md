# Объяснение бизнес-логики калькулятора стоимости номера

- Исходный код: `calculator.py`
- Плохая версия: `bad_calculator.py`
- Тесты: `test_calculator.py`

## 📌 Назначение

Модуль `calculator.py` рассчитывает итоговую стоимость проживания в отеле с учётом множества факторов.

---

## 🔢 Как работает расчёт (по шагам)

### Входные данные

| Параметр | Описание | Пример |
|----------|----------|--------|
| `price_per_night` | Цена за ночь | 5000 руб. |
| `nights` | Количество ночей | 10 |
| `guests` | Количество гостей | 4 |
| `season` | Сезон (PEAK, SHOULDER, OFF_SEASON, NORMAL) | PEAK |
| `extra_service` | Доп. услуга (BREAKFAST, PARKING, SPA) | BREAKFAST |

### Этапы расчёта

```
1. Базовая стоимость = цена_за_ночь × количество_ночей
   ↓
2. + Доплата за лишних гостей (если > 2 человек)
   ↓
3. + Базовая плата за доп. услугу (если выбрана)
   ↓
4. × Сезонный множитель (1.5 для пика, 0.8 для низкого сезона)
   ↓
5. - Скидка за длительность (до 15% при >30 ночей)
   ↓
6. + Надбавка за дешёвые номера (если цена < 3000)
   ↓
7. + Сезонный налог (10% для пика)
   ↓
8. + Фиксированная плата за гостей
   ↓
9. - Скидка за «красивое» число дней (делится на 3, 5, 7)
   ↓
10. + Доп. плата за услугу
    ↓
11. + Бонус за длительность
    ↓
12. + Финальная плата за гостей
    ↓
13. = ИТОГОВАЯ СТОИМОСТЬ
```

---

## 🧩 Детали правил

### Сезонные множители

| Сезон | Множитель | Налог |
|-------|-----------|-------|
| PEAK (пиковый) | 1.5 | 10% |
| SHOULDER (плечевой) | 1.2 | 5% |
| OFF_SEASON (низкий) | 0.8 | 0% |
| NORMAL (обычный) | 1.0 | 0% |

### Доплаты за гостей (сверх 2 человек)

| Превышение | Формула | Пример для 5 гостей |
|------------|---------|---------------------|
| > 2 | (гости - 2) × 500 | 3 × 500 = 1500 |
| > 4 | (гости - 4) × 300 | 1 × 300 = 300 |
| > 6 | (гости - 6) × 200 | — |

**Итого для 5 гостей:** 1500 + 300 = 1800 руб.

### Скидки за длительность

| Ночей | Скидка |
|-------|--------|
| > 30 | 15% |
| > 14 | 10% |
| > 7 | 5% |

### Скидки за делимость

| Делится на | Скидка |
|------------|--------|
| 7 | 500 руб. |
| 5 | 300 руб. |
| 3 | 100 руб. |

*Применяется только первое подходящее условие.*

### Доп. услуги

| Услуга | Базовая | Доп. | Финал |
|--------|---------|------|-------|
| Завтрак (BREAKFAST) | 1000 | 200 | 50 |
| Парковка (PARKING) | 500 | 100 | 25 |
| Спа (SPA) | 1500 | 0 | 0 |

---

## 💡 Предложения по улучшению

### 1. Вынести конфигурацию в отдельный файл или settings

**Проблема:** Все константы «зашиты» в коде.

**Решение:**
```python
# config.py
class PricingConfig:
    SEASON_MULTIPLIERS = {...}
    GUEST_TIERED_RATES = [...]
    # ...
```

---

### 2. Упростить логику гостевых надбавок

**Проблема:** Три отдельные функции с похожей логикой.

**Решение:**
```python
def _guest_fee(guests: int, fee_type: str) -> float:
    """Единая функция для всех типов гостевых сборов."""
    rates = {
        'tiered': GUEST_TIERED_RATES,
        'flat': GUEST_FLAT_FEES,
        'final': GUEST_FINAL_FEES,
    }
    return sum((guests - t) * r for t, r in rates[fee_type] if guests > t)
```

---

### 3. Добавить логирование расчёта

**Проблема:** Непонятно, как получилась итоговая сумма.

**Решение:**
```python
def calculate_total_cost(..., detailed: bool = False) -> float | dict:
    # ...
    if detailed:
        return {
            "base": base,
            "guest_surcharge": guest_fee,
            "season_multiplier": season_mult,
            "total": total,
        }
    return total
```

---

### 4. Валидация через Pydantic

**Проблема:** Ручные проверки `if price_per_night <= 0`.

**Решение:**
```python
from pydantic import BaseModel, Field

class PricingInput(BaseModel):
    price_per_night: float = Field(gt=0)
    nights: int = Field(gt=0)
    guests: int = Field(gt=0)
    season: Season
    extra_service: ExtraService | None = None
```

---

### 5. Стратегия для сезонных расчётов

**Проблема:** Большие словари для каждого параметра сезона.

**Решение:**
```python
class SeasonPricing:
    def __init__(self, multiplier: float, tax: float):
        self.multiplier = multiplier
        self.tax = tax

SEASON_PRICING = {
    Season.PEAK: SeasonPricing(1.5, 0.1),
    # ...
}
```

---

### 6. Документация в docstring

**Проблема:** Нет описания формулы расчёта в функции.

**Решение:** Добавить подробный docstring с примером:

```python
def calculate_total_cost(...) -> float:
    """
    Рассчитывает итоговую стоимость проживания.
    
    Формула:
        1. base = price × nights
        2. + guest surcharges
        3. × season multiplier
        4. - duration discount
        5. + taxes and fees
    
    Пример:
        >>> calculate_total_cost(5000, 10, 4, Season.PEAK)
        87450.0
    """
```

---

### 7. Юнит-тесты для граничных случаев

**Проблема:** Нет покрытия сложных сценариев.

**Решение:** Добавить тесты:
- 1 гость vs 7 гостей
- 7 ночей (делимость) vs 8 ночей
- Пиковый сезон vs низкий
- Все доп. услуги по отдельности

---

### 8. Кэширование для повторяющихся расчётов

**Проблема:** При частых вызовах с теми же параметрами расчёт выполняется заново.

**Решение:**
```python
from functools import lru_cache

@lru_cache(maxsize=128)
def calculate_total_cost(...) -> float:
    # ...
```

---

## 📊 Пример расчёта

**Входные данные:**
- Цена за ночь: 5000 руб.
- Ночей: 10
- Гостей: 4
- Сезон: PEAK
- Услуга: BREAKFAST

**Пошаговый расчёт:**

| Шаг | Описание | Сумма |
|-----|----------|-------|
| 1 | База: 5000 × 10 | 50 000 |
| 2 | Гости: (4-2)×500 | +1 000 |
| 3 | Завтрак (база) | +1 000 |
| 4 | Сезон ×1.5 | ×1.5 = 78 000 |
| 5 | Скидка за 10 ночей (5%) | -3 900 |
| 6 | Надбавка за цену <3000 | 0 (цена > 3000) |
| 7 | Налог 10% | +7 410 |
| 8 | Фикс. плата за гостей | +100 |
| 9 | Скидка за делимость (10 ÷ 5) | −300 |
| 10 | Завтрак (доп. + финал) | +250 |
| 11 | Бонус за длительность (>7 ночей) | +100 |
| 12 | Финал. плата за гостей (4 > 2) | +50 |
| **ИТОГО** | | **81 710 руб.** |

---

## ✅ Вывод

Модуль содержит **сложную бизнес-логику** с множеством взаимосвязанных правил ценообразования. Код написан чисто, но может быть улучшен за счёт:
- Выноса конфигурации
- Упрощения дублирующейся логики
- Добавления детального режима расчёта
- Расширения документации и тестов
